pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/piyushchopade2004/piyush-maven-web-app.git'
            }
        }
         stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
